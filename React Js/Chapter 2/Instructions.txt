to run my-app

cd my-app
npm start

if node modules aren't available, you can get them back using

npm install

to create first react app 

npx create-react-app {name of your app, it should be smaller case}